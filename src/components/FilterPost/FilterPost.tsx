import React, { FC } from 'react';
import css from "./FilterPost.module.scss"

type FilterPostProps = {}

const FilterPost: FC<FilterPostProps> = () => {
  return (
    <div className={css.containerShow}>
    
    </div>
  );
};

export default FilterPost;